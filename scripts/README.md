run per machine: # --- setup-mcp.sh — install and register MCP servers

command:
: chmod +x scripts/setup-mcp.sh && ./scripts/setup-mcp.sh
: chmod +x scripts/install-global.sh && ./scripts/install-global.sh
