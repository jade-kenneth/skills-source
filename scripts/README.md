# Scripts

- `install-global.sh` symlinks skills and commands into `~/.claude/`.
- `setup-mcp.sh` registers the stack's MCP servers for Claude Code.
- `build-agents-md.js` generates a downstream executor contract.
- `validate.sh` runs the repository's local validation suite.

See the root `README.md` for setup and usage.
