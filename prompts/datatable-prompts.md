# DataTable Builder

Canonical instructions live in `../skills/datatable-builder/SKILL.md`.

Invoke the `datatable-builder` skill and provide the target output path. It will
use its bundled `references/` implementation as the architecture source, adapt
unsupported dependencies, preserve the target application's existing visual and
interaction language, and use the shadcn discovery workflow where available.
